import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    input: {
        height: 80,
        width: 240,
        margin: 12,
        borderWidth: 1,
        padding: 10
    },
    button: {
        flex: 2
    },
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }
});